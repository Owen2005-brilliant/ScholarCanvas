# Configuration fixtures

`invalid-publication.js` intentionally contains a duplicate ID, an unsupported publication status, and an unsafe URL. It is used to confirm that `tools/validate_config.py` exits with a non-zero status for bad input. Do not copy it into `data/`.
