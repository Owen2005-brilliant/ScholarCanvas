# Downloadable files

Place your public CV or other downloadable documents in this directory, then update `profile.links.cv` in `data/profile.js`, for example:

```js
cv: "assets/files/cv.pdf"
```

The fictional demo intentionally leaves the CV URL empty so it does not ship a misleading résumé. Empty optional links show an accessible toast instead of navigating.
